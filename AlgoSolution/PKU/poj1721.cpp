#include <iostream>
#include <string.h>
#include <stdio.h>

using namespace std;

int main()
{
  int n, s;
  int End[1005];
  while(scanf("%d%d", &n, &s) != EOF){
    for(int i = 1; i <= n; i ++)
      scanf("%d", End[i]);

  }
  return 0;
}
