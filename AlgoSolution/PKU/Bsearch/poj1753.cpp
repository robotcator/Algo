#include <cstdio>
#include <cstring>

using namespace std;

int main()
{
  /*
  int gaze[5][5];
  char data[5];
  while(scanf("%s", data) != EOF){
    for(int i = 0; i < 4; i ++)
      gaze[0][i] = data[i] == 'b' ? 0 : 1;
    for(int j = 1; j < 4; j ++){
      scanf("%s", data);
      for(int i = 0; i < 4; i ++)
      gaze[j][i] = data[i] == 'b' ? 0 : 1;
    }
    solve();
    }
  }
  */
  int a = 0, b = 1;
  printf("%d\n", a+b);
  return 0;
}
