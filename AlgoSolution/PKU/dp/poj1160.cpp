#include <stdio.h>
#include <iostream>
#include <string.h>

using namespace std;
int m[35][305];

int sovle(int *data, int n)
{

}

int main()
{
  int v, p;
  int data[305];
  while(scanf("%d%d", &v, &p) != EOF{
    for(int i = 1; i <= v; i ++)
      scanf("%d", &data[i]);
    int ans = solve(data, n);
    printf("%d\n", ans);
  }
  return 0;
}
