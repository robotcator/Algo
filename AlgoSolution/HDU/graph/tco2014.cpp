#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;

class WolvesAndSheep{
public:
  int getmin(vector <string> field){
    for()
  }
};

/*
class SpamChecker{
  public:
  string spamCheck(string judgeLog, int good, int bad){
    int sum = 0;
    for(int i = 0; i < judgeLog.size(); i ++){
      if(judgeLog[i] == 'o') sum += good;
      else sum -= bad;
      if(sum < 0) return "SPAM";
    }
    return "NOT SPAM";
  }
};
*/
int main()
{
    return 0;
}
