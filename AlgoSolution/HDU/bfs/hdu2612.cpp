#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;

int main()
{
  char data[205];
  int n, m;
  while(scanf("%d%d", &n, &m) != EOF){
    for(int i = 0; i < n; i ++){
      scanf("%s", data);
      for(int j = 0; j < m; j ++){

      }
    }
  }
  return 0;
}
