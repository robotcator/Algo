#include <cstdio>
#include <cstring>
#include <iostream>
#include <queue>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
  int c, d, n, m, k;
  while(scanf("%d%d", &c, &d) != EOF){
    scanf("%d%d", &n, &m);
    scanf("%d", &k);
  }
  return 0;
}
