#include <cstdio>
#include <cstring>
#include <iostream>
#include <queue>
#include <vector>
#include <algorithm>

using namespace std;

const int maxn = 100000+10;
int a[maxn];

int main(){
  int n, m;
  while(scanf("%d%d", &n, &m) != EOF){
    for(int i = 0; i < m; i ++)
      scanf("%d", &a[i]);
    long long ans;

  }
  return 0;
}
